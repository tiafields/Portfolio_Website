{
    "name": "portfolio-website",
    "scripts": {
        "build": "node build.js",
        "build:watch": "node build.js -w",
        "serve": "npx http-server -p 3000 -c-1 static",
        "start": "concurrently \"yarn build:watch\" \"yarn serve\""
    },
    "type": "module",
    "packageManager": "yarn@4.0.2",
    "devDependencies": {
        "chokidar": "^3.6.0",
        "concurrently": "^8.2.2",
        "ejs": "^3.1.9"
    }
}
